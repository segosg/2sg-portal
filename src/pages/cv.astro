---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="CV | 2sg.io">
  <style>
    /* toutes tes variables et styles CSS ici — SANS les règles nav, footer, body, * */
    :root {
      --bg2: #13161d;
      --border: #1e2330;
      --green: #4ade80;
      --cyan: #67e8f9;
      --pink: #f472b6;
      --text: #cbd5e1;
      --text-dim: #64748b;
      --white: #f1f5f9;
      --accent: #818cf8;
    }

    /* garde tout le CSS SAUF : *, body, nav, footer */
    /* colle ici tout le CSS entre .container et la fin, en excluant nav{} et footer{} */
  </style>

  <!-- colle ici uniquement le contenu entre <body> et </body>, SANS les balises <nav> et <footer> -->
  <div class="container">
    <!-- ... tout le contenu du CV ... -->
  </div>
</BaseLayout>